# Changelog

All notable changes to eSwitch will be documented in this file.

---

## [1.2] - 2026-09-13

### Changed
- **版本号 1.1 → 1.2** — Info.plist/build.sh/菜单/设置页/README badge 同步
- **常驻应用缓存 + 生命周期监听** — 新增 `AppMonitor`：`NSWorkspace` `didLaunch`/`didTerminate` 通知（系统推送，零权限零轮询）增量维护应用缓存，切换器呼出直接读缓存；每次呼出仍全量重建一次作兜底，漏接的通知最多存活一个呼出周期。呼出期间应用退出/启动实时增删卡片（选中位同步修正）；松键激活前对目标做验活（`isTerminated`/⌘H/真实窗口），目标在切走间隙退出/关光窗口时自动改选相邻应用，不再打到死 pid 上
- **卡片首字母改用应用英文名** — 卡片顶部首字母与「字母快速定位」的匹配改用应用英文名（`CFBundleDisplayName`/`CFBundleName` 原始值，不随系统语言本地化），不再用中文名的拼音首字母；卡片/面板显示名仍为本地化中文名。无英文名（或全为非字母）的应用回退 `•` 占位符
- **移除跳桌面方案（合成 ⌃+数字）** — 跨空间激活不再跳转桌面，统一使用系统原生 `activateAllWindows`（Cmd+Tab 语义，把窗口拉回当前 Space），失败退化为普通激活；Finder 激活同样不再跳转桌面；删除 `⌃+数字` 合成、空间扫描、桌面索引缓存与 `--selftest-ctrl` 自检，不再要求启用系统「切换桌面」快捷键
- **GPU 占用优化** — 移除卡片双层阴影（切换动画时每帧重算高斯投影，是 GPU 峰值主因）、倒影高斯模糊，以及面板/卡片的 3 层渐变叠加，改为单层低密度纯色渲染；持续与峰值 GPU 占用显著下降，视觉基本不变（系统窗口阴影仍提供深度感）
- **恢复两侧卡片 3D 斜切** — 重新启用侧卡绕 Y 轴透视旋转，保留 Compiz Ring 的环形纵深观感
- **移除弃用 API** — `NSWorkspace.launchApplication("Finder")` 改为 `openApplication(at:configuration:)`，消除 macOS 11+ 编译警告

### Fixed
- **过滤无窗口的应用** — 切换列表过滤重写（替代 AX 查询方案）：保留退出中进程（`isTerminated`）与 ⌘H 隐藏应用的排除；真实窗口判定改用全窗口列表（`CGWindowListCopyWindowInfo .optionAll`）按 pid 分组一次判定——菜单栏占位窗（细窄满屏宽，每个应用都有 4-8 个）不算真实窗口；离屏窗口仅当 CGS 查到归属空间环才算（= 其他桌面的窗口，跨空间切换不受影响）；关窗后保留的隐藏窗口（无空间归属）不算。老系统无 CGS 空间符号时退化为旧行为，窗口枚举失败保守列入
- **切完无窗应用自动移出列表（自愈标记）** — 激活后轮询仍无上屏窗口的应用打标记，下次呼出排除（处理个别隐藏窗口仍被 CGS 归属到 Space 的残留）；应用开出/还原出上屏窗口后自动恢复，无需手动清理；新增 `--selftest-windows <应用名>` 诊断（窗口 layer/alpha/尺寸/空间归属/真实窗口判定）
- **无法切换到访达** — `getApps()` 原本显式过滤掉 `com.apple.finder`，访达永远不出现在切换列表；移除该过滤，访达恢复可切换
- **面板四角直角阴影** — 投影由直接作用于含 `NSVisualEffectView` 的复合视图改为独立圆角矩形 `.shadow()`，避免按矩形边界投影导致四角露出直角阴影/底色

---

## [1.1] - 2026-09-11

### Added
- **字母快速定位** — 切换器显示时按住呼出修饰键（`⌘`）按字母（如 `A`）直接跳到该首字母应用（中文取拼音首字母）：
    - 同首字母多个应用时其余留在原环位并显示高亮角标，再按同一字母在候选间轮换
    - 跳转后继续按原快捷键退出定位态、正常循环切换；松键激活环心应用
- **卡片首字母角标** — 卡片内部顶部显示应用名首字母（`#EB6217`），中文取拼音首字母，emoji/符号自动跳过
- **build.sh 职责收敛** — 只负责构建 + 签名 .app；DMG 打包独立为 `scripts/package-dmg.sh`（可单独重打包）
- shell 脚本统一移入 `scripts/` 目录，路径基于脚本位置解析，任意目录可调用

### Changed
- 版本号 1.0 → 1.1

### Fixed
- 字母跳转后原快捷键切换不生效：普通旋转/反向切换现在会退出字母定位态

---

## [1.0] - 2026-09-11

### Added
- **环形应用切换器** — 按住 `⌘`+`esc` 呼出，每按一次切换到下一个应用，松开即激活；中间卡片正对放大，两侧卡片绕 Y 轴透视倾斜、下沉，带地面倒影，配合弹簧动画顺滑轮播
- **玻璃质感切换面板** — 半透明深色圆角面板（纯 SwiftUI 圆角路径绘制，圆角外 100% 透明）+ 细描边 + 系统贴合阴影（彻底消除四角直角灰底）；面板顶部显示当前应用名，底部进度指示点（当前应用高亮拉长）
- **自定义快捷键设置** — 「设置...」中「呼出/切换」（默认 `⌘ esc`）与「反向切换」（默认 `⇧⌘ esc`）均可点击按钮后按下新组合键重新录制；快捷键以 JSON 持久化到 UserDefaults，重启保留，修改后立即生效；录制时暂停全局事件监听、自动检测两个快捷键之间的冲突，按 Esc 可取消录制
- **面板屏幕选择设置** — 「设置...」中新增「面板屏幕」选项：**主屏幕**（默认，固定主显示器）/ **跟随光标所在屏**（按鼠标所在显示器定位）；切换器显示时单独按 `esc` 取消（不激活应用）
- **跨虚拟桌面切换** — 目标应用在其他桌面时自动跳转过去（合成 ⌃+数字，绝对定位不累积误差，修复 uuid 大小写导致索引失效、切换后后台自动翻桌等缺陷）
- **开机自启** — 「设置...」中可开启登录项自动启动（SMAppService，macOS 13+ 回退 SMLoginItem）
- 应用图标更新（icns 全尺寸，圆角化，托盘图标重绘为模板矢量）

### Fixed
- 应用更名为 eSwitch：包/目标/图标/目录重命名，bundle ID 改 `com.mfyang.eswitch`
- GPU 优化：移除流光动画，中间卡片改为静态白色高亮边框；优化切换动画 GPU 占用
- 过滤无窗口/正在退出/隐藏的应用，只列真实可切换的 GUI 应用
- 无法切换到访达 — 早期显式过滤掉 Finder，移除该过滤，访达恢复可切换
